services:
  - type: web
    name: frederick-backend
    env: node
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm --filter backend build
    startCommand: pnpm --filter backend start
